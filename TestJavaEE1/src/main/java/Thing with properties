чтобы запустить профиль сборки
mvn compile -P env-dev(название профиля)
mvn compile -Denv=develop
чтобы активировалось по дефолту нкжно добавить в activation
<activeByDefault>true</activeByDefault>
для другой хуйни создаём в settings.xml <activeProfiles>

<depndencyManagement> для уточнения того какая версия зависимости будет использоваться если
другие зависимости могут использовать её разные версии. написал хз как но знай об этой хуйне

также у проекта можно создавать разные модули для разделения ответственности, тоесть разделять логику приложения между собой,
при этом, чтобы импортировать классы из другого модуля, нужно добавить его в тэг зависимостей в pom.xml